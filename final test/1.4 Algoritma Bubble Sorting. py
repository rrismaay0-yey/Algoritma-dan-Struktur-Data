def bubble_sort(arr):
    n = len(arr)
    for i in range(n-1):
        for j in range(n-1, i, -1):  # mulai dari belakang
            if arr[j] < arr[j-1]:
                arr[j], arr[j-1] = arr[j-1], arr[j]
    return arr

data = [8, 4, 2, 9, 1]
print("Hasil sorting:", bubble_sort(data))

